# Diagnostic run (see agopx/learners/neural.py module docstring): tested whether
# decaying only W_1 (not W_2) removes the circulant-deviation drift -- it didn't.
learner: nn
run_id: nn_x_plus_y_p61_r0.5_seed0_w1only_decay

operation: "x+y"
p: 61
training_fraction: 0.5
width: 1024
epochs: 50
batch_size: 32
lr: 0.001
weight_decay: 1.0
w2_weight_decay: 0.0
snapshot_every: 1
seed: 0
