Reference material only — not imported by `agopx`.

- `rfm-grokking/` — clone of https://github.com/nmallinar/rfm-grokking (GPL-3.0). Used to
  cross-check the RFM/AGOP update rule and the discrete-log reordering (`utils.py`) while
  reimplementing from `project.md` and the paper. If any code is ever lifted verbatim or
  near-verbatim into `agopx/`, that specific file must carry a GPL-3.0 header and the project
  as a whole should be treated as GPL-3.0 for that component — see project.md Section 2.
